# Khattak TV

Android TV application project for Khattak TV.

## Build

The `develop` branch includes a GitHub Actions workflow named **Build Khattak TV APK**. It builds the debug APK and uploads it as the `khattak-tv-debug-apk` artifact.

## Streams

Channel URLs are placeholders until authorized public stream URLs are provided in `ChannelRepository.kt`.
